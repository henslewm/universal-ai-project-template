# Project State

- **Status:** TEMPLATE MAINTENANCE — #8 REOPENED (ADR-021); PR #22 in flight on `issue-8-codex-findings`, awaiting a clean Codex review of its current head and the maintainer's explicit merge approval
- **Last verified:** 2026-09-14 UTC
- **Active branch:** `issue-8-codex-findings` (PR #22); `main` is at `0c8fc8c` and still carries the bypassable gate
- **Controlling scope:** [Locked master #14](https://github.com/henslewm/universal-ai-project-template/issues/14); title/body/order unchanged.
- **Next unblocked child:** none until #8 closes again. [Issue #9](https://github.com/henslewm/universal-ai-project-template/issues/9) is next afterwards and has not been started; its earlier dogfood run went through the unfixed gate and is provisional (ADR-021).

## Verified foundation

Issues #2 through #7 are closed through merged PRs #15 to #20. #8 was merged through PR #21 at `226353c6` and closed, then **reopened** (ADR-021): Codex's review of the merged head posted 14 seconds before the closure with two P1 and two P2 findings in `scripts/acceptance.py`, so the closure record claimed gates that were bypassable. PR #22 answers them and everything review found since: 15 Codex reviews and 26 findings through `97e3ef6`, then a stop-for-diagnosis (ADR-024) because the lifetime and filesystem-trust classes kept recurring — they are now invariants with one owner each rather than point checks — and ADR-025, which lets accepted work complete GitHub publication (the #6 ledger refused any feedback ledger not still at `REVIEW_PENDING`, the exact status `sync-feedback` leaves behind).

#8 delivered the independent acceptance controller in the reserved gap between the feedback controller's `REVIEW_PENDING` and the packet's `REVIEW → ACCEPTED` transition. `ACCEPTANCE_PROTOCOL.md` is the contract; `scripts/acceptance.py` keeps one append-only hash-chained ledger per reviewed task, independently re-executes the contract's declared validation commands (the one deliberate exception to the metadata-only pattern, ADR-014), renders bounded minimal-context review packets from ledger records only, enforces the five reviewer verdicts with structural refusals, holds a hard cross-family gate with recorded waiver at high and critical risk, and grants acceptance only when every gate required by the risk floor has a current-submission passing record. Gate floors are assigned in the work packet and can only be tightened; contract repair can no longer change risk or the review block. Decisions are ADR-014 through ADR-017 from the first pass and ADR-021 through ADR-025 from the reopening, and `docs/ISSUE_8_VALIDATION.md` holds the acceptance mapping, the two-round dogfood in which #8's own diff was rejected by a minimal-context reviewer for a real gate-bypass defect and accepted only after the regression-tested correction, and the environmental record. The 11-event dogfood ledger is preserved at `_acceptance-demo` beside the repository.

This is an unactivated reusable template under explicit maintenance authority. No real project registry, provider calls, credentials or permission changes are required. Generated projects should link their generated structured project-state index here and audit it against GitHub; task narration belongs in canonical issue comments.

## Continuation

Finish #8 first: PR #22 needs a Codex review whose `commit_id` equals its current head with every finding answered, CI green, and the maintainer's explicit merge approval; then re-close #8 with a completion comment naming the limitations carried forward (template packaging: generated projects inherit a payload-drift check they cannot pass, obsolete payload files are never detected, and development decision history ships into fresh projects — all outside #8, recorded here and in the handoff, no issue drafted at the maintainer's choice). Only then begin #9 from master #14 and #9 only, in a fresh session. Domain-specific rigor is #9's subject — the software/hardware template must distinguish simulated success from actual hardware-in-loop verification, which is exactly the distinction the acceptance controller's machine-runnable-versus-attested validation split now supports. Model-performance calibration remains #12 and should receive both #7's run-4 profile and #8's review-economics observations. The stray `prompts/PROVISION_LOCAL_MODEL.md` in the working tree is the maintainer's local-model-provisioning draft awaiting his deliberate commit (it looks like #12 input). Continue one child at a time. Do not edit the locked master or delete construction branches outside the later authorized cleanup.
