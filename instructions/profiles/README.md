# Model / Effort / Tool Profiles

These files follow the convention requested for specialized behavior:

`model_effort_tool_INSTRUCTIONS.md`

Select a profile only when it materially fits the task. The universal and platform masters still control. Do not load every profile into every session.
