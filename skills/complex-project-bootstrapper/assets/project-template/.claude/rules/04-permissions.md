# Permissions

Connector or tool availability is not authority for a consequential action. Read-only is the default. Require explicit user direction before sending communications, publishing, filing, purchasing, deleting, changing permissions, or modifying external records.
