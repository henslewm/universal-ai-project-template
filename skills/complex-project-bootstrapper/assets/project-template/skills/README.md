# Project Skills

The canonical portable skill source lives here. Native copies are synchronized to:

- Codex: `.agents/skills/`
- Claude Code: `.claude/skills/`

Run `python scripts/sync_skills.py` after editing a canonical skill, then run the validator.

Use `docs/SKILL_DECISION_MATRIX.md` before adding another skill.
