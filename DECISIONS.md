# Decision Log

Append material decisions. Do not rewrite prior decisions without recording supersession.

| ID | Date | Decision | Rationale | Alternatives considered | Consequences | Status |
|---|---|---|---|---|---|---|
| ADR-000 | 2026-08-29 | Use GitHub as the durable project state and native instruction files as platform adapters | Enables cross-model continuity and reviewable history | Chat-only memory; separate vendor projects | Requires disciplined closeout and commits | Accepted |
| ADR-001 | 2026-09-12 | Separate generation, foundation review and explicit activation; bind approval to configuration, governing documents, domain and workflow as well as architecture | Closes normal bootstrap and stale-approval bypass paths in Issue #2 | Treating generated files as activation; binding only architecture JSON | Re-review is required after bound changes; initialized-project rebootstrap is refused to preserve records | Accepted within Issue #2 |
| ADR-002 | 2026-09-12 | Hash governing Markdown as UTF-8 text with normalized LF line endings | Normal Git checkout/clone must preserve accepted authority across Windows and Linux | Raw-byte document hashes | Content changes invalidate approval while line-ending normalization alone does not | Accepted within Issue #2 |
| ADR-003 | 2026-09-12 | Use one schema-backed packet with full versioned contract snapshots and revision-bound lifecycle events; latest snapshot is the sole current contract | Provides common machine-readable scope and traceable changes without a second editable issue contract | Separate handwritten issue contracts; silent in-place contract updates | Supported revision commands preserve history, reset readiness and generate the issue view from the current snapshot | Implementation decision within Issue #3 |
| ADR-004 | 2026-09-12 | Keep work-packet tooling metadata-only and install its JSON Schema validator separately from standard-library bootstrap | Delivers #3's contract/lifecycle validation without introducing a provider, executor or another autonomy mechanism | Eager dependency for every bootstrap; live worker/provider implementation in #3 | Actual execution must still enforce bootstrap, permissions, evidence and review gates; packet validation is not authentication | Implementation decision within Issue #3 |
| ADR-005 | 2026-09-12 | Route from the explicit worker tier path or separate reviewer tier, with risk/complexity floors, deterministic economic estimates and saved replay inputs | Implements #4 without silently expanding the architect's authority or binding contracts to vendors | Traversing every tier; cheapest token price alone; clamping reviewer tier to worker maximum | Independent role histories/budgets and finite fallback; disabled synthetic defaults; live execution and observed calibration remain their later owning issues | Implementation decision within Issue #4 |
| ADR-006 | 2026-09-12 | Use one append-only task event ledger with exclusive reservations before worker intent; retain frozen cumulative limits across contract repairs and finite diagnosis/recovery allowances | Prevents supported restart/concurrency/provider/revision paths from resetting bounded work and preserves failure evidence | Stateless retry counters; overwriting state; resetting on revision; treating all holds as resumable blockers | Controller state derives from replay; final human-decision holds are sticky; live execution/accounting and GitHub reconstruction remain separate integrations | Implementation decision within Issue #5 |
| ADR-007 | 2026-09-12 | Store canonical task/issue bindings and complete packet/feedback history on a separate GitHub state branch; claim publication by file-SHA comparison before posting | Preserves one machine authority and recoverable issue projections without duplicate writes after uncertainty | Chat passdowns; independently editable issue contracts; marker-only comment deduplication | Bounded Contents API storage, explicit configured authority, no automatic retry of unknown POSTs; live execution and substantive acceptance stay separate | Implementation decision within Issue #6 |
| ADR-008 | 2026-09-13 | Express both ledger dead-end repairs as recorded, evidence-gated state transitions rather than operator file edits: a PR binding is replaced only by appending the outgoing number to `superseded_prs`, and a publication claim is retired only by appending it to `released` after proving no comment exists | Independent review showed a rejected PR and a lost claim write each froze a task permanently, and the prescribed manual repair could only be performed by bypassing the validator | Leaving both to out-of-band file edits; allowing the PR and claim bindings to be freely rewritten; deleting or editing comments to recover | Recovery stays inside the validated append-only record and remains auditable; accepted rows and merged PRs are still immutable, a visible receipt is still never releasable, and each repair costs one growing list entry | Implementation decision within Issue #6 |
| ADR-010 | 2026-09-13 | Keep the execution harness a preparation-and-ingestion adapter that never invokes a model or runs the harness command, bind routed resources to harnesses by configuration, and close a reservation spent on an unpreparable harness as PROVIDER_UNAVAILABLE | Issue #7 requires bounded worker execution without letting the harness become the architect or a second source of truth, and a reservation that cannot be prepared must not be left pending or silently retried | Executing the harness from the adapter; letting Cline own routing; releasing the spent reservation; leaving the attempt pending | Live runs are operator actions whose transcripts are evidence for that run only; a harness that cannot be prepared escalates to the architect; Cline is substitutable without invalidating packets or contracts | Implementation decision within Issue #7 |
| ADR-011 | 2026-09-13 | Close a dispatched attempt that produced no usable report only through an explicit `abandon` with a stated reason, never by inferring the absence of a report file | A live operator run showed a dispatched worker can produce nothing at all, which left the reservation pending forever; inferring failure from a missing file would repeat the mistake ADR-009 corrected | Treating a missing report as an automatic failure; leaving the attempt pending; releasing the reservation | The stated reason becomes the attempt's evidence and fingerprint; unknown scope moves the controller to an architect decision instead of looping a structurally incapable worker | Implementation decision within Issue #7 |
| ADR-012 | 2026-09-13 | Refuse a dispatch whose brief, worker rules, declared harness overhead and reserved output cannot fit the context window the routed resource is actually served in, using a per-harness declared overhead and an optional per-binding served window | A live operator run failed on its first tool call because the harness system prompt alone consumed most of an 8192-token window, which the router could not see: it checks only the architect's declared tokens against the resource's declared window | Trusting the router's capacity check alone; shrinking the brief until it happened to fit; tokenizing the brief inside the harness; letting the worker discover the limit at run time | Preparation refuses with the arithmetic stated and closes the spent reservation as PROVIDER_UNAVAILABLE; the estimate is a conservative character approximation, so an understated overhead or overstated served window can still admit an impossible dispatch | Implementation decision within Issue #7 |
| ADR-009 | 2026-09-13 | Supersede the claim-release half of ADR-008: publication commits a distinct `posting` phase before any POST, a `claimed` entry is resumable because it is durable proof no POST was attempted, and an attempted publication is never retired or retried | Review showed full comment pagination proves only present absence, so retiring a claim on "no visible comment" could double-publish once an accepted comment became visible | Keeping `release` with a visibility delay; allowing an operator override flag; deleting the duplicate comment afterwards | The original stranding path is fixed without evidence-free retirement; the residual uncertain case is narrower, is documented, and deliberately has no command | Accepted within Issue #6; supersedes ADR-008 on claim release only |
| ADR-013 | 2026-09-13 | Extend the ADR-012 capacity check with an operator-declared `context_growth_reserve_tokens`, refuse a binding that overstates its served window before a reservation is spent, and bound and scan the indented brief the worker is handed rather than only its canonical form | Review of the ADR-012 implementation found three gaps: the check covered the opening prompt only while the passing run's context grew from 5,480 to 13,649 tokens, a configuration contradiction cost a bounded attempt and escalated to the architect, and the configured brief bound measured a form no worker reads | Predicting growth inside the harness; tokenizing the transcript; leaving the contradiction to the capacity path; bounding the canonical form only | A dispatch sized to its first turn alone is refused unless it also has declared room to grow; the reserve is a declaration calibrated from observed runs, not a prediction, so an understated reserve still admits a doomed dispatch | Implementation decision within Issue #7; extends ADR-012 |
| ADR-014 | 2026-09-13 | Let the acceptance controller execute the contract's declared validation commands itself — the one deliberate exception to the metadata-only tool pattern — bounded to exact argv without a shell, a workspace-confined cwd, per-check timeouts, output caps and a credential scan that refuses to record tainted output | Issue #7 proved a worker report is supplied evidence, not proof; independence requires observing the checks run, not transcribing a claim that they ran. The gate's first live use contradicted a supplied clean-run claim with an observed payload-drift failure | Keeping every tool metadata-only with operator-run rechecks; a hybrid per-validation execution flag | The gate executes only architect-committed commands and never invokes a model or harness; a validation without a command fails closed to recorded operator attestation, and an implementer cannot attest its own check | Implementation decision within Issue #8 |
| ADR-015 | 2026-09-13 | Declare risk-tier acceptance-gate floors once in the work-packet layer; a packet's optional review block and project configuration may only add gates, and a contract repair can no longer change risk or the review block | Review requirements must be assigned in the packet without letting any supported path — packet authoring, configuration, or the repair mechanism that exists to fix wording — quietly lower the gates a risk classification requires | Full per-packet gate lists validated against a minimum; floors in operator-editable configuration; leaving risk repairable as before | Loosening is structurally impossible rather than prohibited; lowering gates now requires a recorded contract revision under architect authority, which resets readiness | Implementation decision within Issue #8 |
| ADR-016 | 2026-09-13 | Refuse high- and critical-risk acceptance when the approving reviewer's declared model family matches any implementation actor's, unless an explicit waiver with operator and reason is recorded; the waiver becomes acceptance evidence and is cleared by resubmission | Same-family review demonstrably missed defects on #6 and #7 that cross-family review caught; master goal 9's 'when practical' needs a forcing function that leaves an auditable trace instead of a silent lapse | A soft warning; an absolute requirement with no waiver, which stalls a resource pool with one reachable strong family | Identities remain recorded declarations the controller cannot authenticate, and the protocol says so; an implementer cannot record the waiver for its own work | Implementation decision within Issue #8 |
| ADR-017 | 2026-09-13 | Bind review-gate satisfaction to the current submission — a gate approval recorded before a resubmission cannot satisfy that gate for the resubmitted artifact — and let a bounded rejection overturn the worker's objective PASS routing outcome so the corrective attempt is routable inside the frozen budget | The independent dogfood review of #8's own diff found that a pre-resubmission model-review approval plus a post-resubmission architect approval could close a submission the model-review gate never examined; separately, the router's terminal-PASS stop blocked dispatching corrective work after a rejection | Clearing review history on resubmit, which would destroy repeated-rejection fingerprints; relaxing the router's terminal-PASS rule itself | The full review history still feeds attempt counting and fingerprints; the raw worker result is preserved while its routing outcome records the overturn, the same pattern as blocker resolution | Implementation decision within Issue #8; found by minimal-context independent review |
| ADR-018 | 2026-09-13 | Apply domain rules as a per-profile module registered in `work_packet.DOMAIN_MODULES`, invoked inside `validate_contract` on every path that validates a contract (packet validation, feedback contract repair, acceptance init) and at the acceptance ATTESTATION event; a domain module adds structure and refusals over the `domain` block and can never relax the common contract or a controller gate | Issue #9 needs the software-hardware extension to be enforced structure rather than a label, without duplicating what the controllers already own or opening a second validation path a caller could choose to skip | A standalone domain validator nobody is obliged to run; per-profile forks of the controllers; widening the common schema with hardware-specific fields every profile would carry | Two small hooks in existing controllers; a profile with no registered module is unchanged; the legal profiles can register their own rules in #10/#11 through the same registry | Implementation decision within Issue #9 |
| ADR-019 | 2026-09-13 | Hardware status is earned, never authored: a contract may declare only `UNVERIFIED_ON_HARDWARE` or `NOT_HARDWARE_FACING`, `VERIFIED_ON_HARDWARE` in a contract is refused, and the earned status is derived from the acceptance ledger — `VERIFIED_ON_HARDWARE` only for an `ACCEPTED` ledger whose hardware-rung validations were all attested against a digest-bound hardware evidence record | Master goal 10 forbids claiming hardware success from compilation or simulation; a status an author can type is a label, while a status computed from recorded gate outcomes cannot be asserted ahead of the evidence | A mutable `hardware_status` field updated by the worker or architect; a status recorded by a separate ledger; trusting the attestation's free-text evidence alone | A machine-only acceptance reports `UNVERIFIED_ON_HARDWARE` however green it was; the record the attestation binds is an operator declaration the controller cannot authenticate, and the protocol says so | Implementation decision within Issue #9 |
| ADR-020 | 2026-09-13 | Bind the verification ladder to the acceptance controller's command split: every software-hardware validation maps to one rung; rungs static through integration must declare a `command` and rungs hardware_in_loop and field must not; a hardware-rung attestation must carry the lines derived from a validated hardware evidence record with a `pass` outcome and the attesting operator as its recorder, and a failed observation is never attested | The split #8 shipped (executed command versus non-implementer attestation) is exactly the hardware distinction #9 must encode; tying rungs to it means a simulator cannot masquerade as hardware by declaring a command, and an attestation cannot stand in for a runnable check by omitting one | Per-validation free-form `kind` labels; letting hardware rungs optionally run a command; accepting any attestation text at hardware rungs | Both misclassifications are refused at contract validation rather than discovered at review; the hardware evidence record is the unit of hardware evidence and its digest is what the ledger binds | Implementation decision within Issue #9 |
| ADR-021 | 2026-09-13 | Reopen #8 rather than file a follow-up: bind an acceptance decision to its task revision and reviewed result at both the forwarding step and the feedback ledger's REVIEW event; refuse a reviewer whose declared tier is below the contract's `reviewer_tier`; refuse any symlink entry, file or directory, before building the workspace digest; and enforce the check-output bound while the process runs by streaming into bounded buffers and hashing the full stream | PR #21 was merged and #8 closed 14 seconds before Codex posted two P1 and two P2 findings on its final head, so #8's completion record claimed gates that were demonstrably bypassable; a follow-up issue would have left that record standing | A new issue with the fixes deferred to #13; a soft warning on tier mismatch; ignoring directory symlinks as out of scope | The standing rules now require a Codex review of the current head SHA and answered findings before any merge, forbid advancing the next child while findings are open, and name GitHub as the only source for findings; the #9 dogfood run made through the unfixed gate is provisional until repeated | Accepted within reopened Issue #8 |
| ADR-022 | 2026-09-14 | Treat the presence of `binding` and `result_dispatch_id` on a REVIEW event as the version boundary: a stored event without them replays under the pre-binding shape with no binding check, while a new decision without them is refused; and kill a check's process tree whenever pipe draining is abandoned, on a normal exit as well as a timeout, using a session/process group on POSIX and a job object on Windows | Codex's second pass on PR #22 showed that requiring the binding unconditionally would make every existing feedback ledger with a REVIEW event unreplayable, and that a descendant left behind by a normally exiting check could hold the pipes and accumulate as an orphan | A migration that rewrites stored events, which would break the hash chain; a ledger-level schema version stamp, which no existing ledger carries; waiting on descendants indefinitely; polling process trees by name | Old ledgers keep their audit and recovery paths; new decisions cannot omit the binding; abandoned drains are recorded as not completing within the bound with the observed digest | Accepted within reopened Issue #8 |
| ADR-023 | 2026-09-14 | Apply the ADR-022 boundary to the reviewer-tier rule: the acceptance ledger's `apply` knows whether an event is stored, a stored `REVIEW_OPEN` below the contract's `reviewer_tier` replays marked with `tier_shortfall`, a stored `ACCEPT` on such a review stands, and a new opening below the tier or a new acceptance resting on a marked review is refused | Codex's review of PR #22 showed the tier check, added in this PR, would make every existing acceptance ledger with a permitted-at-the-time weaker opening unreplayable, losing status, audit and decision forwarding | Enforcing the tier on replay and accepting the loss; a ledger schema-version stamp no existing ledger carries; treating the stored review as unsatisfied even for an already recorded acceptance, which would re-break replay at the ACCEPT event | The same stored-versus-new pattern now covers both ledgers; the mark is visible in state and review packets rather than silent; a pending ledger recovers by opening a review at the required tier, and the marked opening does not count against the review attempt budget, so that recovery always has a slot | Accepted within reopened Issue #8 |
| ADR-024 | 2026-09-14 | Treat workspace trust and check-process lifetime in the deterministic gate as invariants established in one place each: `trusted_workspace` refuses `..`, a linked root and any linked ancestor before `resolve()` and then scans the whole tree, every check's `cwd` passes the same component inspection before it is resolved, and `ProcessTree.run` owns a check from launch to confirmed-stopped so the tree is ended on every exit — timeout, normal exit with a descendant alive, refusal and any exception — with the bounded drain terminating the group when abandoned | Rounds 1–14 on PR #22 answered 21 lifetime and trust findings at the point each was noticed; every patch left the next exit path or the next path component unguarded and review found it. `bounded_capture` had no `try/finally` between creating the tree and closing it, and `run_checks` resolved each `cwd` before that iteration's scan | Continuing per-finding patches, demonstrably nonterminating; a full sandbox, beyond the gate's reach | The invariants hold for what the gate controls: a descendant that calls `setsid()` and a link created, read and removed inside one command remain outside them and the protocol keeps naming deployment isolation as the control; a new exit path or path source now has one place to hook; the review loop rule is recorded as R-012 | Accepted within reopened Issue #8 |
| ADR-025 | 2026-09-14 | Let the GitHub ledger extend and publish a feedback ledger whose status is `ACCEPTED` as well as `REVIEW_PENDING`, still by reviewer/integrator events only and never when held or unfinished | Codex's repository review reproduced acceptance → `sync-feedback` `ACCEPTED` → GitHub ledger refusal (`Cannot extend a held or unfinished feedback packet`): the #6 adapter predates #8's forwarding of the decision into the feedback ledger, so recording an acceptance made the task unpublishable and #8's 'task closes only after required gates pass' criterion could not be completed end to end | Leaving the feedback ledger at `REVIEW_PENDING` and never forwarding the decision, which discards #8's binding; a new issue on master #14, which would have left #8's closure claiming a path that does not work | One end-to-end regression now runs the whole closing path (`run_checks` → review → `accept` → `sync_feedback` → integrator `VERIFIED` → `row_valid`); the legacy `REVIEW_PENDING` path remains valid for rows written before the forwarding existed | Accepted within reopened Issue #8 |
| ADR-026 | 2026-09-14 | Make the domain profile a required argument of `validate_contract` (a registered profile string; `None` refused), close the `software-hardware` domain block (`additionalProperties: false` with the two optional keys the examples use declared) and refuse the literal `VERIFIED_ON_HARDWARE` in any string the block carries | The #9 dogfood reviewer observed that a `profile=None` default skipped the domain rules for any caller that forgot the argument, and that an open block let the earned status be written into free text where no rule looked; both are silent bypasses of ADR-018/019 rather than refusals | Keeping the default and auditing callers; a warning on the skipped path; scanning only the declared free-text field | Every existing caller now names its profile; generic tests pass a profile with no registered rules; the example contract loses its one undeclared key; `UNVERIFIED_ON_HARDWARE` is matched as a whole word so the declared status is not a claim | Accepted within Issue #9 |
| ADR-027 | 2026-09-14 | `software_hardware.py status` reports its `evidence_basis` — `attestation` without `--evidence-dir`, where the declared digests are reported as attested and the reason says they are not record-verified, and `record` with it — and record verification compares the record's operator to the attesting operator as well as task, validation, rung and outcome | The reviewer observed that `status` without an evidence directory printed `VERIFIED_ON_HARDWARE` on attested digests alone without saying so, and that a record found by digest was never compared to the attester: the `operator=` line is checked at append time against the attesting operator, but the digest line is only a string then, so an attester could bind another operator's record and copy their own name into the line | Refusing `status` without an evidence directory; a fourth status value for attested-only; comparing the operator at append time by opening the record, which the controller cannot do without the directory | The verdict and its basis are both in the output and the CLI; the forgery case is a regression; `evidence_path` is present on every entry | Accepted within Issue #9 |
| ADR-028 | 2026-09-14 | Check a packet's declared scope against what its files actually import — every `synth_bridge` module a packet's named files import is owned by the packet or listed as a consumed interface, and every consumed interface's owner is a declared dependency — and make the example packets truthful rather than loosen the check: the adapter's tests speak raw transport bytes, the device-through-adapter crossing moves to the workflow packet, and the workflow packet declares the codec and the device packet's test double it consumes | The dogfood reviewer rejected `AC-SAMPLE`: the adapter packet declared only the transport interface while its tests imported the codec and drove the device through the adapter, the workflow packet omitted two modules its integration test imported, and the scoping test checked path shape rather than imports, so the contradiction was invisible to the gate | Declaring the extra dependencies on the adapter packet, which would have made a framing-only adapter depend on the codec; keeping the path-shape test | The scope check parses imports with `ast` and has a negative control for both an undeclared import and an undeclared dependency; the sample suite is 28 tests | Accepted within Issue #9; found by minimal-context independent review |
| ADR-029 | 2026-09-14 | Record verification in `software_hardware.py status --evidence-dir` treats the digest as proof of which bytes were bound and nothing more: the found object must validate against `$defs/hardware_evidence`, must be for this task, validation and rung with a `pass` outcome and the attesting operator, and every line the attestation carried (`device`, `firmware`, `observed_at`, `level`, `outcome`, `operator`) must agree with the record's field; each disagreement is named in the reason and the record is still reported as found (`evidence_path` set) but not verified | Codex's cross-family review of the #9 dogfood rejected `AC-EXTENSIONS`: `_find_record` accepted any dictionary with a matching canonical digest, so a five-field object carrying only the binding fields, with the attestation's device, firmware and time lines typed by hand, earned record-basis `VERIFIED_ON_HARDWARE`; and a complete record whose metadata contradicted the attested lines survived verification because those lines were never compared. The same-family round approved the same diff | Validating the record at `attest` time, which the controller cannot do without the directory; refusing `status` outright on an invalid record instead of reporting it unverified; comparing only the fields the old check compared | The attestation's lines are now a contract with the record, not decoration; the five-field stub and each metadata mismatch are regressions; the boundary that the record is still an operator declaration (R-013) is unchanged | Accepted within Issue #9; found by cross-family minimal-context review after same-family approval |
| ADR-030 | 2026-09-15 | Answer Codex round 1 on PR #34 in one batch: an attestation carrying a recognized `key=` line more than once is refused at parse time rather than resolved by position, and a stored attestation that no longer parses is reported unverified by `status` instead of replayed as good; the sample adapter assembles a frame across partial port reads until its declared length arrives or the caller's `timeout_s` expires, refusing the frame by declared length before reading its body; and the published `config/bootstrap.schema.json` names the same nine software-hardware fields as `validate_bootstrap.DOMAIN_FIELDS`, with a regression that binds the two | `parsed_evidence` kept the first of duplicate keys, so `outcome=pass` followed by `outcome=fail` attested a failed observation as passing while the protocol says every attested line is verified (P1); the adapter's single `read(length + 1)` returned a truncated frame when a port delivered fewer bytes, breaking its own "one whole frame by declared length" rule, and ignored `timeout_s` entirely (two P2); the schema still listed the three legacy fields, so schema-driven tooling accepted a package the validator refuses (P2) | Keeping last-wins; per-port timeouts configured outside the adapter; leaving the schema as documentation only | The rule that every attested line is verified now has no positional escape; the sample's host rules are true as stated; the schema and validator cannot drift apart silently | Accepted within Issue #9; Codex findings on PR #34 |
| ADR-031 | 2026-09-15 | Stop-for-diagnosis on the sample adapter (R-012): rounds 1 and 2 on PR #34 each returned framing/timeout findings answered where noticed. The invariant, owned in one place: the port contract carries the deadline — `Port.read(size, timeout_s)` returns what arrived within `timeout_s` — and `SerialAdapter.receive` either returns one whole frame by its declared length or, having consumed any byte of a frame it could not complete, closes the port before raising; a timeout that consumed nothing leaves the port open, and the declared length is checked before the body is read | The adapter cannot bound a read it does not control, so a `read(size)` contract lets a blocking port outlast the caller's deadline whatever the adapter checks afterwards; and a partial frame left in the port turns the next receive into the cross-frame corruption the framing rule exists to prevent. Patching the read loop and the deadline check separately left each other's gap open | Polling reads with a sleep loop (round 1's answer); resynchronizing by scanning for a TAG byte, which invents protocol behavior the specification does not state; leaving the port open and documenting the hazard | `Port` gains a timeout argument and the SHB-04 packet's port assumption says so; the fake ports in the sample tests honor it; close-on-incomplete-frame joins the packet's declared host rules beside short-write and oversized-frame closure; regressions cover a blocking port bounded by the remaining time and a partial frame that closes the port | Accepted within Issue #9; Codex round 2 on PR #34 |
| ADR-032 | 2026-09-15 | Apply the ADR-022/023 stored-versus-new boundary to domain rules: a stored INIT whose contract fails the profile's domain rules replays marked with `domain_shortfall` (the errors) and only a new INIT is refused; a stored ATTESTATION the profile rule would refuse replays marked with `domain_shortfall` and only a new one is refused; `software_hardware.py status` reports a marked attestation as unverified and refuses to derive a hardware status from a marked contract with the reason stated; and record verification compares device, firmware, observation time, level and outcome exactly, case-folding only the operator | Codex round 2 on PR #34: the #8 dogfood ledger `_acceptance-demo`, whose contract predates the structural domain block, no longer replays at all (`fixture_protocol`, missing `component`…), contradicting `apply`'s stored-event contract; a stored attestation with duplicate keys, accepted before the round-1 refusal, raises inside `apply` before `status` can mark it; and case-folding every evidence field verified `SN-ABC` against `SN-abc`, reporting the wrong physical unit as record-verified | Refusing legacy ledgers and accepting the loss of #8's audit trail; a ledger schema-version stamp no existing ledger carries; exact comparison of the operator too, which the controller's own actor rule already normalizes | Every ledger written before the rules replays for audit, extension and acceptance status; the shortfall is visible in `status` and the review packet, never silent; the hardware status is the one derivation that refuses, because it has nothing lawful to derive from | Accepted within Issue #9; Codex round 2 on PR #34 |
| ADR-033 | 2026-09-15 | Give the ADR-031 started-frame invariant one owner: everything `SerialAdapter.receive` does after the first byte of a frame is consumed runs under a single guard that closes the port on any exception leaving the call — timeout, oversized frame, a port read that raises — so no exit path can be added without the closure | Round 3 on PR #34 found the exception path: ADR-031 stated the invariant but the code implemented it as a `close()` before each timeout raise, and a body read that raised (a disconnect) left the port open with a started frame in it. This is the ADR-024 diagnosis again — an invariant stated once and implemented as point checks — and the class had now recurred across three rounds | Adding `close()` to the exception path where noticed; catching only `OSError` | The regression injects an exception from a port read after the header and asserts the adapter is closed and the exception propagates; a read that raises before any byte is consumed leaves the port open, as the invariant states | Accepted within Issue #9; Codex round 3 on PR #34; extends ADR-031 |
| ADR-034 | 2026-09-15 | Every hardware evidence record enters through one loader, `load_record`, which refuses a JSON object with a duplicate key at any depth, and `_find_record` names a file it refused instead of silently skipping it; the record schema's `observed_at` is a `date-time` format validated through a format checker registered on the domain validator, so an impossible calendar value is refused as well as a malformed shape | Round 1's duplicate-attestation-line finding and round 3's duplicate-record-key finding are the same class: a value read from evidence resolved by position rather than refused. `json.loads` keeps the last of duplicate keys, so a file carrying `"outcome": "fail"` then `"outcome": "pass"` hashed and verified as passing. Separately the timestamp pattern accepted `2026-99-99T99:99:99Z` because `_validator` supplied no format checker | Scanning for duplicates in the CLI only; leaving the timestamp to the pattern | The loader is the choke point for `_find_record`, the `hardware-evidence` command and the tests; a refused file is visible in `status --evidence-dir`'s reason rather than absent | Accepted within Issue #9; Codex round 3 on PR #34 |
| ADR-035 | 2026-09-15 | Widen the ADR-033 owner from `receive` to the adapter's whole contact with the port: every port call in `send` and `receive` runs under one guard, `_wire`, and any exception leaving it — a raising write, a raising read, a short write, an oversized declaration, a frame incomplete at the deadline — closes the port before propagating. The one thing that does not close the port is a receive that timed out having consumed nothing, which is a quiet line, not a failure | Round 4 on PR #34: a write that raised after a partial transmission left the port open while the short-write path closed it. ADR-033 had given the started-frame closure one owner inside `receive` only; the send path had the same point-check shape. Stating the invariant per method kept producing the next unguarded method | A second guard in `send`; treating a raising port as recoverable | One guard, one regression per method; a read that raises before the header now closes the port too (a broken port is not a quiet one), which supersedes that half of the ADR-033 regression | Accepted within Issue #9; Codex round 4 on PR #34; extends ADR-031 and ADR-033 |
| ADR-036 | 2026-09-15 | Closing the adapter is unconditional and owned by `close` alone: the adapter forgets the port before asking the driver to close it, so a driver whose `close()` raises cannot leave the adapter open, and `_wire` propagates the original wire failure with a failing close attached as its cause rather than masked | Round 5 on PR #34: `close` assigned `_port = None` only after the driver's `close()` returned, so a driver that raised on close during `_wire`'s failure handling left `is_open` true and the next call reused the port, and the close exception replaced the wire failure. The fifth adapter round; the invariant's owner was right, its own teardown was not unconditional — the ADR-024 launch-error lesson in miniature | Swallowing close errors; retrying close | `close` clears the reference first; a raising close is still raised, chained under the original failure; regression with a driver whose close raises | Accepted within Issue #9; Codex round 5 on PR #34; extends ADR-035 |
| ADR-037 | 2026-09-15 | Apply the stored-versus-new boundary (ADR-032) to the work-packet layer and give the evidence loader ownership of the digest: `validate` checks stored revisions against the common contract only and `domain_shortfall` reports which revisions fail the profile's rules, while `create` and `revise` refuse a new contract that fails them, so a packet authored before the rules can still be transitioned and revised into compliance; `load_record` refuses `NaN`/`Infinity` constants and `_find_record` treats a digest failure as a refused file, so one bad file never aborts `status --evidence-dir` | Round 5 on PR #34: `validate` re-ran the domain rules over every historical revision, so a packet with a formerly valid free-form `domain` block failed `require_valid` and could not even append the correcting revision — the same class ADR-032 closed for the acceptance ledger, on the other path; and `json.loads` admits `NaN` and overflowing literals that `evidence_digest` then rejects outside the per-file handler, aborting the scan | A migration command that rewrites revisions, which would break every hash; enforcing the rules at transition time as well | The packet layer has the same three-way split as the ledger: stored replays, shortfall reported, new refused; the feedback repair path already validates a new revision; the loader is the only place a record's text is interpreted, digest included | Accepted within Issue #9; Codex round 5 on PR #34 |
| ADR-038 | 2026-09-15 | A hardware-rung attestation is exactly the lines `hardware-evidence` prints for its record — one digest line and the six recognized `key=value` lines — and nothing else: `parsed_evidence` returns every line it did not recognize and `validate_attestation` refuses any such line, whitespace variants included, so there is no line in a hardware-rung attestation that is not verified against the record. Free text belongs in the record's `test_setup` and `observed_behavior`, which the digest binds | Round 7 on PR #34 is round 1's and round 3's class a third time: the parser skipped a `key=value` line whose key it did not recognize, so ` outcome=fail` (leading space) rode along beside `outcome=pass` while the invariant says every attested line is verified. Each earlier answer refused one way of carrying a second value; the invariant is that there is no unverified line at all | Normalizing whitespace and re-parsing; allowing free text but scanning it for recognized keys | Attestations at machine rungs are unaffected (the controller refuses them anyway); the #8-era test fixture that mixed a narrative line with the record lines is corrected; a stored attestation with an extra line replays marked under ADR-032 | Accepted within Issue #9; Codex round 7 on PR #34 |
| ADR-039 | 2026-09-15 | An attestation element is one line and the record fields that become lines are single-line by schema: `device_identity`, `firmware_version` and `operator` use a `single_line` definition (non-blank, no CR or LF), and `validate_attestation` refuses any element containing CR or LF before parsing it, so the seven lines ADR-038 names are seven physical lines | Round 8 on PR #34, the evidence class a fourth time: `nonblank` admitted `SN-1\noutcome=fail`, `evidence_lines` placed it in one element, the parser partitioned once, and the exact comparison against the same record agreed — an injected physical line verified. ADR-038's "exactly seven lines" had counted elements, not lines | Rejecting CR/LF only at parse time; normalizing line breaks | The record schema and the attestation rule agree on what a line is; a stored attestation with an embedded break replays marked under ADR-032 | Accepted within Issue #9; Codex round 8 on PR #34; extends ADR-038 |
| ADR-040 | 2026-09-15 | `validate_bootstrap.DOMAIN_FIELDS` is the single source of a profile's orientation fields, and every published listing of them — `config/bootstrap.schema.json` and the bootstrapper's `references/intake-schema.md` — is tested against it, so a listing cannot drift from the validator without a failing test | Round 1 (the JSON schema) and round 8 (the intake reference) on PR #34 were the same class: a published contract that still named the three legacy fields while the validator required nine, so a package prepared from the document reached an activation refusal. Fixing each listing where noticed left the next one stale | Generating the documents from the validator, which would hide the prose; leaving the reference as narrative | The intake reference names the nine fields; a regression greps each key of every profile's `DOMAIN_FIELDS` in the reference and compares the schema's required set; the three copies are kept identical by `sync_skills.py` | Accepted within Issue #9; Codex rounds 1 and 8 on PR #34 |
| ADR-041 | 2026-09-15 | The stored-versus-new boundary for domain rules is a property of every path that validates a contract, not of the paths where it was noticed: the feedback ledger's stored `REPAIR_CONTRACT` diagnosis replays with its shortfall recorded in `domain_shortfall`, `work_packet.revise` takes `domain_rules` so a stored repair's revision is not re-judged, and a new repair is refused as before; with ADR-032 (acceptance INIT and ATTESTATION) and ADR-037 (packet revisions) every contract-validating path now threads `stored` to the domain rules | Round 10 on PR #34 found the third path: a pre-#9 feedback ledger whose stored repair used the formerly valid free-form block could not replay, because the repair branch validated with today's rules and `revise` enforced them again. Closing the boundary path by path — rounds 2, 5, 10 — was the ADR-024 pattern; the invariant is that the switch exists wherever `validate_contract` is reached from a replay | A migration that rewrites stored repairs, breaking the chain; a feedback-level schema stamp no ledger carries | `domain_shortfall` is visible in `summary`; the regression writes a legacy repair on the chain by hand and shows replay, the mark, and refusal of the same repair as new | Accepted within Issue #9; Codex round 10 on PR #34; extends ADR-032 and ADR-037 |
| ADR-042 | 2026-09-15 | The one-poll allowance belongs to the `receive` call, not to each read within it: a receive makes its first port contact unconditionally (a zero timeout polls what has already arrived), and after that first contact no port read follows a passed deadline, whichever part of the frame it would have fetched — so a header that arrived in time whose body did not is a started frame that closes the port (ADR-031), never a frame returned late | Round 11 on PR #34 is the deadline class a third time (rounds 2, 10, 11): ADR-031 handed the port the deadline, round 10 stopped re-reading after an empty read, and round 11 found the allowance reset at the body read, because `_read_within` held "first read" per call to itself. Each answer held the property per read; the invariant is per receive | Dropping the allowance entirely, which makes `receive(0)` unable to poll; polling once more for the body with a zero timeout, which is the late frame again | `_read_within` takes `first_contact`; the header read passes `True`, the body read `False`; the regression returns the header just before the deadline, delivers the body after it, and shows the port asked once more at most and the started frame closed | Accepted within Issue #9; Codex round 11 on PR #34; completes ADR-031 |
| ADR-043 | 2026-09-15 | A hardware evidence record carries the work-packet contract `revision` it was observed against, and record verification (`record_problems`, the `--evidence-dir` basis) refuses one that does not match the acceptance ledger's own `binding.revision`, alongside the existing `task_id` check | Round 12 on PR #34: task_id and validation_id identity were checked, but nothing tied a record to which contract revision it was produced under; a revision that keeps the same task, validation ID and rung (a repair that does not touch that check) let hardware evidence from a changed, earlier implementation silently verify the later one, contrary to `WORK_PACKET_PROTOCOL.md`'s requirement that a later verifier check the task revision | Binding by `contract_hash` as well, which the codebase's own binding convention (ADR feedback/model-router bindings) pairs with revision; deferred because every fixture in this suite starts a fresh packet at revision 1, so a hash check would exercise nothing beyond what revision already proves here | The schema requires `revision` (integer, minimum 1) on every record; the example and every test fixture declare it; the regression revises the packet by a repair to revision 2 and shows a revision-1 record refused against the new binding while a matching-revision record still verifies | Accepted within Issue #9; Codex round 12 on PR #34 |
| ADR-044 | 2026-09-15 | A hardware evidence record also carries the exact `contract_hash` of the revision it was observed against, not only its `revision` number, and `record_problems` checks both against the acceptance ledger's binding | Round 13 on PR #34, immediately after ADR-043: revision numbers are lineage-local, so two independently revised variants of the same task can both be "revision 2" with different content; a record bound only by revision number could verify the wrong variant. This was flagged as a deferred alternative in ADR-043 and confirmed necessary the same round | Binding by contract_hash alone, dropping revision -- kept both because the revision number is the more informative half of a mismatch reason for a human reading `status` | The schema requires `contract_hash` (sha256) alongside `revision`; the regression revises the same base packet twice with different content, both landing on revision 2, and shows a record bound to one variant refused against the other's ledger | Accepted within Issue #9; Codex round 13 on PR #34; extends ADR-043 |
| ADR-045 | 2026-09-15 | The example hardware evidence record's `revision` and `contract_hash` are bound to `packets/SHB-04-adapter.contract.json` revision 1's actual fingerprint, and a regression checks it against `work_packet.fingerprint` directly, so the documented operator walkthrough (README `SRC-HIL-PROCEDURE`) genuinely round-trips through `status --evidence-dir` rather than only the standalone `hardware-evidence` command | Round 14 on PR #34: ADR-043/044 added `revision` and `contract_hash` to the schema, and the illustrative example was given a fictional-but-valid hash disconnected from the packet file it documents, so the canonical example silently could not demonstrate the record-verification flow the README describes | Generating the value at doc-build time, which the project has no such step for; leaving the field fictional with a note, which contradicts the walkthrough's own commands | The example record now verifies end to end against a freshly created SHB-04-adapter packet at revision 1 | Accepted within Issue #9; Codex round 14 on PR #34 |
| ADR-046 | 2026-09-15 | `validate_attestation` takes the ATTESTATION event's own timestamp and refuses a hardware-rung attestation whose declared `observed_at` is later than it, regardless of whether a hardware evidence record is ever supplied to re-verify the attestation; `parse_timestamp` (factored out of the existing `date-time` format checker) parses both sides as real calendar values | Round 15 on PR #34: nothing compared the attested observation time to when it was attested, so a record dated in the future (or any later than its own attestation) passed structural and record-basis checks alike, letting an accepted ledger report `VERIFIED_ON_HARDWARE` for an observation that had not yet occurred | Checking only on the record basis (`--evidence-dir`), which leaves the attestation-basis path exposed exactly like the gap this closes; deriving the bound at ledger-replay time instead of at append time, which would let a since-passed future date silently become valid on replay | The regression appends an attestation dated after the event and shows it refused; a stored attestation predating the rule still replays marked, per ADR-032 | Accepted within Issue #9; Codex round 15 on PR #34 |
| ADR-047 | 2026-09-15 | A hardware evidence record also carries the `dispatch_id` of the result and the `sha256` of the artifact it was observed against, and `record_problems` checks both against the acceptance ledger's current `result`/`artifact`, alongside `revision` and `contract_hash` | Round 16 on PR #34: contract identity does not identify which implementation was tested. `RESUBMIT` replaces the result and artifact while keeping the same contract and revision and clearing prior attestations; without this, an unchanged evidence file could be re-attested after a rejection and silently verify the resubmission it was never observed against | Refusing every RESUBMIT while a hardware rung is unattested, which forecloses a legitimate corrected resubmission entirely; deriving a submission identifier from the contract instead, which is exactly the property that does not distinguish two submissions under one contract | The schema requires both fields; the regression rejects an attested submission, resubmits with a different result and artifact, and shows the unchanged evidence refused against the resubmission while a record made against the new result and artifact verifies | Accepted within Issue #9; Codex round 16 on PR #34; extends ADR-043/044 |
| ADR-048 | 2026-09-15 | `single_line` caps at 1991 characters, leaving room for the longest key an attestation line prefixes onto it (`firmware=` or `operator=`, 9 characters) within acceptance's 2000-character `bounded_text` element limit; `identifier` and `sha256` use the strict end assertion `(?![\s\S])` in place of a bare `$`, matching `config/acceptance.schema.json`'s own idiom | Round 17 on PR #34, two P2s found together: an unbounded `device_identity`/`firmware_version`/`operator` could validate as a record and then be refused at attestation once its key prefix pushed it over acceptance's own element limit; and `identifier`/`sha256`'s `$`-anchored patterns admitted a trailing newline the same way ADR-039 found for `single_line`, so `task_id`, `contract_hash`, `dispatch_id` and `artifact_sha256` could carry one and validate while differing from the strictly compared ledger binding | A per-field maxLength keyed to each field's own shortest prefix, which is more precise but couples three fields' bounds to which key names ship them; leaving `observed_at`'s field-level pattern as is, since its `date-time` format checker already parses with `fullmatch` and is unaffected in practice | A record at the new bound still attests within acceptance's limit; a record over it, or carrying a trailing newline in any of the five affected fields, is refused before it reaches attestation or record verification | Accepted within Issue #9; Codex round 17 on PR #34; extends ADR-039 |
| ADR-049 | 2026-09-15 | A hardware-rung attestation also carries the ledger's current `dispatch_id` and `artifact_sha256` as two more of the lines `hardware-evidence` prints (extending ADR-038's invariant from six recognized lines to eight), and `validate_attestation` refuses one that does not match the acceptance ledger's current result and artifact at append time, not only when a record is later re-verified through `record_problems` | Round 18 on PR #34: ADR-047 closed the record-basis gap but left the attestation-basis path open -- without `--evidence-dir`, an operator could re-attest a rejected submission's unchanged record after a resubmission and have it reported VERIFIED_ON_HARDWARE without the comparison in `record_problems` ever running | Refusing every attestation for the rest of a resubmitted ledger's life, which RESUBMIT's existing attestation-clearing already achieves without a new rule; comparing only on the record basis, which is exactly the gap this closes | Every attestation, on either basis, is now bound to the submission it was actually observed against; `record_problems`'s own dispatch_id/artifact_sha256 checks (ADR-047) stand as a second, independent check against the record file itself | Accepted within Issue #9; Codex round 18 on PR #34; extends ADR-038 and ADR-047 |
| ADR-050 | 2026-09-15 | `status` refuses to derive `VERIFIED_ON_HARDWARE` for a contract whose `domain.synthetic` is `true`, regardless of attestation or acceptance state, and excludes an attested hardware rung on such a contract from `highest_level_satisfied` for the same reason | Round 18 on PR #34: every canonical SHB example and dogfood packet declares `synthetic: true` because its inputs are fictional, but nothing checked that flag before this -- an attested illustrative record could still be reported as physical verification, contradicting the profile's central promise that a simulated pass never becomes a hardware claim | Refusing `synthetic: true` together with a hardware rung at contract-authoring time, which would forbid a synthetic packet from ever declaring a hardware assumption to exercise the domain machinery against, exactly what the dogfood packets need to do | A synthetic contract's machine-runnable rungs still report normally; only the hardware-rung ceiling is capped, with the reason naming `synthetic: true` explicitly | Accepted within Issue #9; Codex round 18 on PR #34 |
| ADR-051 | 2026-09-15 | `ACCEPTANCE_PROTOCOL.md`'s "Domain rules at the gates" section names `dispatch_id` and the artifact digest among the lines a hardware-rung attestation carries, and states that `status` never derives `VERIFIED_ON_HARDWARE` for a contract declaring `domain.synthetic: true` | Round 19 on PR #34: ADR-049 and ADR-050 changed what the protocol document had described as normative -- an operator following the unrevised text could construct an attestation `validate_attestation` now refuses, or expect `VERIFIED_ON_HARDWARE` a synthetic contract can never earn | None; the document must describe the implemented contract | The distributed template copy carries the same text via `sync_skills.py`; no code changed | Accepted within Issue #9; Codex round 19 on PR #34 |
| ADR-052 | 2026-09-15 | An acceptance ledger's `submitted_at` (the INIT event's timestamp, refreshed by every RESUBMIT to that event's own timestamp) is a lower bound on a hardware attestation's `observed_at`, alongside `attested_at` as the existing upper bound (ADR-046): an observation cannot predate the submission it claims to have tested | Round 20 on PR #34: checking `dispatch_id`/`artifact_sha256` alone (ADR-047/049) still let a genuinely matching record carry a stale `observed_at` copied from an earlier submission, so an operator who updated the two identifiers as instructed but left the timestamp could still earn `VERIFIED_ON_HARDWARE` for an observation that predated the resubmission it was supposedly made against | Anchoring the lower bound to the latest CHECKS run instead of the submission, which is stricter and was not what the finding described; leaving it unchecked and relying only on operator discipline | `submitted_at` is derived fresh on every replay from the INIT or latest RESUBMIT event already on the chain, so no ledger schema change or migration is needed; the regression suite's fixtures, which used a fixed illustrative `observed_at`, now compute one live at attestation time | Accepted within Issue #9; Codex round 20 on PR #34; extends ADR-046 |
| ADR-053 | 2026-09-15 | `acceptable` refuses a new (not-yet-stored) `ACCEPT` that would rest on a domain-shortfall-marked attestation -- one a profile's rules would now refuse but which replays marked, not raised, under ADR-032 -- while replaying an `ACCEPT` already on the chain over the same shortfall still succeeds, preserving that acceptance as history | Round 21 on PR #34: `deterministic_status` counts a shortfall-marked attestation as ATTESTED so a historical acceptance keeps replaying, but `acceptable` never distinguished a new decision from a replayed one, so a ledger that had only reached `GATES_PENDING` before the rule existed could still be freshly accepted today on evidence the current rules explicitly reject | Refusing every ledger with any shortfall-marked attestation outright, which would also un-accept ledgers already closed on one, contradicting ADR-032's promise that stored history is preserved | A stored `ACCEPT` over a shortfall-marked attestation still replays `ACCEPTED`; a new one is refused until the operator attests again to satisfy today's rules | Accepted within Issue #9; Codex round 21 on PR #34; extends ADR-032 |
| ADR-054 | 2026-09-15 | `acceptance.artifact_identity(artifact)` combines an artifact's `reference` and `sha256` into one digest, and every place a hardware evidence record binds to "the current artifact" (ADR-047/049) uses this identity instead of `artifact["sha256"]` alone | Round 22 on PR #34: `checked_artifact` permits empty `content` for a `reference`-kind artifact, so its `sha256` is then the fixed empty-content digest regardless of what `reference` names; a resubmission could change the referenced commit while `sha256` and `dispatch_id` both stay the same, and an old hardware record would still verify against the new reference | Requiring non-empty content for reference-kind artifacts, which defeats the point of a reference (an oversized diff that cannot be embedded); adding a new record field for `reference` directly, which duplicates what combining already achieves | The hardware evidence schema's `artifact_sha256` field is unchanged in shape (still a plain sha256) but its description states what it now binds; the regression shows two reference-kind artifacts sharing one empty-content digest still produce two different identities | Accepted within Issue #9; Codex round 22 on PR #34; extends ADR-047 and ADR-049 |
| ADR-055 | 2026-09-15 | `templates/software-hardware/PROFILE.md` and its identical `DOMAIN_PROFILE.md` copy state the `synthetic: true` ceiling on `VERIFIED_ON_HARDWARE` (ADR-050) alongside the derivation they already described | Round 23 on PR #34: both documents still said acceptance plus full attestation earns `VERIFIED_ON_HARDWARE` unconditionally, contradicting `status()`'s actual behavior for every worked example packet, all six of which declare `synthetic: true` | None; the document must describe the implemented contract | Documentation only; no code changed; the two copies are kept identical by `sync_skills.py` | Accepted within Issue #9; Codex round 23 on PR #34 |
| ADR-056 | 2026-09-15 | A hardware-rung attestation also carries the record's own `validation_id` as one more of the lines `hardware-evidence` prints (extending ADR-038/049's line count from eight to nine recognized lines), and `validate_attestation` refuses one that does not match the attestation's own declared target; `record_problems` compares the same field for the record basis | Post-merge independent review of main after PR #34: the printed evidence lines omitted `validation_id`, so a schema-valid record made for one hardware-rung check could be appended unchanged for a different check in the same submission -- level, device, firmware, observed_at, outcome, operator, dispatch_id and artifact_sha256 can all legitimately match across two checks. Reproduced with a room-temperature record attested against a maximum-temperature check | None; every other identity dimension already binds the submission and the contract, but none of them names which check was observed | Regression attests a two-hardware-rung contract's room-temperature record against its maximum-temperature check and shows it refused, then shows the same record verifying the check it actually names | Accepted within Issue #9 follow-up; post-merge independent review; extends ADR-038 and ADR-049 |
| ADR-057 | 2026-09-15 | `SerialAdapter.receive`'s body read earns the same unconditional first contact the header read always gets, exactly when the whole `receive()` call is itself a zero-timeout poll (`first_contact=timeout_s == 0`); a positive timeout keeps ADR-042 | Post-merge independent review of main after PR #34: ADR-042 made the body's first read conditional on remaining time regardless of the requested timeout, so `receive(0)` against a port with the whole frame already buffered read the header, then refused to look at the already-buffered body and raised a timeout for a complete frame that was there from the start | Removing the `first_contact` distinction entirely, which reopens the original ADR-042 exploit (a body that only became available strictly after a real, positive-timeout deadline scooped up as if received in time) | A zero-timeout receive() over a fully buffered frame now returns it complete; a zero-timeout receive() with only the header buffered still correctly times out; the original ADR-042 regression (a positive timeout, body arrives after the deadline) is unaffected | Accepted within Issue #9 follow-up; post-merge independent review; completes ADR-042 |
| ADR-058 | 2026-09-15 | A zero-timeout `receive()` samples `Port.available()` once, before touching the port at all, as a fixed byte budget for the whole call, and a new `_read_polled` never draws more than that budget total however many non-blocking `read(size, 0.0)` calls a trickling port takes to hand it over; `_read_within` is unchanged and now serves only a positive timeout | PR #35 review of ADR-057's own fix: making the body's first contact unconditional whenever `timeout_s == 0` reopened the ADR-042 hazard from the other direction (a body that only became available strictly after the header read could still be returned as received in time) and never fixed a pre-existing bug (a port handing back an already-fully-buffered frame in small chunks still timed out, because a zero-timeout deadline is already in the past by the second call). The reviewer's own reproduction proves why: no two of a port's own read calls, however fast, can be assumed to have happened at the same instant, so no wall-clock measurement between them can ever tell "the rest of what was already buffered" from "a byte that arrived after the poll" | A wall-clock elapsed-time epsilon between reads, which the reproduction shows is the wrong tool even in principle and would make the regression suite timing-dependent; looping while a read keeps returning non-empty with no other bound, which drains a trickling port correctly but is exactly as willing to drain a port that hands over genuinely late data on its very next call, since both look identical to the loop | `Port` gains `available() -> int` (the `in_waiting`/`bytesAvailable()` primitive real serial APIs already expose); `FakePort` and `FakeSensorPort` implement it. Regressions: a `TricklePort`-chunked, fully pre-buffered frame is now assembled complete at `receive(0)`; a header present at the poll instant whose body becomes available only afterward (driven by an explicit call count, never real time) still times out and closes the port (ADR-031). SHB-04-adapter's Port-contract input, host rules and hardware assumptions now name `available()`, and the pinned example hardware evidence record's `contract_hash` is recomputed to match | Accepted within Issue #9 follow-up; PR #35 review; extends ADR-031, ADR-042 and ADR-057 |
