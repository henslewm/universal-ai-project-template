{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Software + hardware domain extension",
  "description": "Profile-specific structure for a software-hardware contract's `domain` block and for the hardware evidence record that a hardware-rung attestation must bind. The common contract is enforced independently; this schema adds structure and never loosens it. Runtime rules in scripts/software_hardware.py add the cross-field checks: every validation id is mapped to exactly one rung, rungs at or below integration require a declared command, hardware rungs forbid one, and VERIFIED_ON_HARDWARE is never authored in a contract.",
  "$defs": {
    "nonblank": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S"
    },
    "single_line": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S",
      "not": {
        "pattern": "[\\r\\n]"
      },
      "description": "Non-blank and on one physical line: no CR or LF anywhere, stated as a prohibition because an end anchor matches before a trailing newline."
    },
    "identifier": {
      "type": "string",
      "minLength": 1,
      "maxLength": 80,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,79}$"
    },
    "sha256": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    },
    "component": {
      "type": "string",
      "enum": [
        "protocol_codec",
        "transport",
        "device_abstraction",
        "hardware_adapter",
        "configuration",
        "persistence",
        "business_logic",
        "ui_api",
        "simulator_fake",
        "telemetry_logging",
        "deployment_operations"
      ],
      "description": "The one separation category this packet implements. A packet spanning several is not independently testable and must be decomposed."
    },
    "validation_level": {
      "type": "string",
      "enum": [
        "static",
        "unit",
        "contract",
        "simulation",
        "integration",
        "hardware_in_loop",
        "field"
      ],
      "description": "One rung of the verification ladder. static through integration are machine-runnable and must declare a command; hardware_in_loop and field are observed on physical hardware and must not."
    },
    "declared_hardware_status": {
      "type": "string",
      "enum": [
        "UNVERIFIED_ON_HARDWARE",
        "NOT_HARDWARE_FACING"
      ],
      "description": "What a contract may declare. VERIFIED_ON_HARDWARE is never authored; it is derived from an accepted acceptance ledger whose hardware-rung validations were all attested with bound evidence."
    },
    "contract_domain": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "component",
        "hardware_status",
        "hardware_assumptions",
        "protocol_references",
        "validation_levels"
      ],
      "properties": {
        "component": {
          "$ref": "#/$defs/component"
        },
        "hardware_status": {
          "$ref": "#/$defs/declared_hardware_status"
        },
        "hardware_assumptions": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "assumption",
              "source_id"
            ],
            "properties": {
              "assumption": {
                "$ref": "#/$defs/nonblank"
              },
              "source_id": {
                "$ref": "#/$defs/identifier"
              }
            }
          },
          "description": "Every behavior of physical hardware this packet relies on, each citing a contract source. Empty means the packet relies on none and is NOT_HARDWARE_FACING."
        },
        "protocol_references": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/identifier"
          },
          "uniqueItems": true,
          "description": "Contract source ids for the protocol or interface specifications the packet implements against. Required non-empty when any hardware assumption exists."
        },
        "validation_levels": {
          "type": "object",
          "propertyNames": {
            "$ref": "#/$defs/identifier"
          },
          "additionalProperties": {
            "$ref": "#/$defs/validation_level"
          },
          "minProperties": 1,
          "description": "Exactly one ladder rung per contract validation id; no other ids."
        },
        "synthetic": {
          "type": "boolean",
          "description": "True when every input is fictional and establishes no real hardware behavior."
        },
        "template_child_issue": {
          "type": "integer",
          "minimum": 1,
          "description": "For this template's own dogfood packets only: the child issue the packet reviews."
        }
      },
      "description": "Closed: no free-text field exists in which a hardware claim could be written; hardware status is carried by hardware_status alone and VERIFIED_ON_HARDWARE is refused wherever it appears."
    },
    "hardware_evidence": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "schema_version",
        "task_id",
        "validation_id",
        "level",
        "device_identity",
        "firmware_version",
        "test_setup",
        "expected_behavior",
        "observed_behavior",
        "outcome",
        "observed_at",
        "operator",
        "artifacts"
      ],
      "properties": {
        "schema_version": {
          "type": "string",
          "const": "1.0"
        },
        "task_id": {
          "$ref": "#/$defs/identifier"
        },
        "validation_id": {
          "$ref": "#/$defs/identifier"
        },
        "level": {
          "type": "string",
          "enum": [
            "hardware_in_loop",
            "field"
          ]
        },
        "device_identity": {
          "$ref": "#/$defs/single_line",
          "description": "Exact model, revision and serial or asset identifier of the physical unit observed."
        },
        "firmware_version": {
          "$ref": "#/$defs/single_line"
        },
        "test_setup": {
          "$ref": "#/$defs/nonblank",
          "description": "Physical connections, host, tooling and environment during the observation."
        },
        "expected_behavior": {
          "$ref": "#/$defs/nonblank"
        },
        "observed_behavior": {
          "$ref": "#/$defs/nonblank",
          "description": "What was actually observed, in enough detail to be contradicted."
        },
        "outcome": {
          "type": "string",
          "enum": [
            "pass",
            "fail"
          ]
        },
        "observed_at": {
          "$ref": "#/$defs/nonblank",
          "format": "date-time",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}[Tt][0-9]{2}:[0-9]{2}:[0-9]{2}(?:\\.[0-9]+)?(?:[Zz]|[+-](?:[01][0-9]|2[0-3]):[0-5][0-9])$",
          "description": "When the observation was made, RFC 3339; validated as a real calendar value, not only a shape."
        },
        "operator": {
          "$ref": "#/$defs/single_line",
          "description": "The person who ran the hardware and will record the attestation; never an implementation actor."
        },
        "artifacts": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "reference"
            ],
            "properties": {
              "reference": {
                "$ref": "#/$defs/nonblank"
              },
              "sha256": {
                "$ref": "#/$defs/sha256"
              }
            }
          },
          "description": "Logs, captures, photographs or measurements, by reference and digest where integrity matters. Never the artifact content itself."
        }
      },
      "description": "The structured record a hardware_in_loop or field attestation binds by digest. It is an operator's declaration the controller cannot authenticate; the digest makes it auditable and immutable once attested."
    }
  }
}
